-- ============================================================
-- إعادة بناء مخطط قاعدة البيانات (schema public)
-- ⚠️ يمسح كل الجداول والبيانات بشكل نهائي.
-- يُشغَّل مرة واحدة عند تغيير أنواع enum (College) قبل نشر الكود الجديد.
--
-- الطريقة: افتحي SQL Console في مزوّد قاعدة البيانات (مثل Neon) والصقي هذا الملف.
-- بعدها أعيدي تشغيل التطبيق (أو انشري نسخة جديدة) ليعيد create_all الجداول،
-- ثم شغّلي الزرع:  python -m app.seed_data
-- ============================================================

DROP SCHEMA IF EXISTS public CASCADE;
CREATE SCHEMA public;
GRANT ALL ON SCHEMA public TO public;
